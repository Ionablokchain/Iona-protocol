name: slsa-provenance

on:
  push:
    branches: [ "main" ]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  actions: read
  id-token: write

jobs:
  provenance:
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.1.0
    with:
      base64-subjects: |
        cGtnOmdlbmVyaWMvaW9uYUAke3sgZ2l0aHViLnNoYSB9fQ==
