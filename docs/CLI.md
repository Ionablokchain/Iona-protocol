# CLI

A minimal CLI for node ops and dev UX should include:
- keygen / import / export
- status (peers, height, finality)
- tx submit
- governance: propose/vote
- staking: delegate/undelegate/withdraw

This package includes a placeholder `iona-cli` binary. Wire it to your RPC.
