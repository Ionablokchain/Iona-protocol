# Simulation & property testing

To compete, you need deterministic simulations:
- random partitions, message delays, node crashes
- invariant checks: no conflicting finality
- replayability (seeded)

This package includes placeholders in `tests/` and suggestions for a simulator crate.
